# AtelierX 작업 규칙

- 2026-09-22 생성 화면 후속 승인: 작품>캐릭터>의상 및 카테고리>조각 트리를 명확히 표시하고 체크박스 크기를 보정한다. Seed는 -1이면 이미지별 무작위, 0 이상이면 지정값이며 실제 사용값은 Core가 고정·기록한다. 업스케일 모델 목록 선택과 예상 최종 해상도를 제공한다. 제작 계획은 생성만/단일 검사/단일+묶음 검사를 선택하며 미검사 결과를 합격으로 취급하지 않는다. Terra 분담으로 구현·검증한다.

- 2026-09-22 후속 사용자 승인: 작업실을 캐릭터 관리와 이미지 생성으로 분리한다. 외형은 캐릭터 appearance_prompt, 의상은 상의·하의·액세서리이며 조각의 accessories 포함 체크는 기본 true다. 생성은 의상 체크 트리 × 전역 조각 체크 트리와 설정의 3단 구성, 총 장수·예정 목록을 제공한다. 검토와 갤러리를 분리하되 수동 승인을 강제하지 않는다. 목업 재작성 없이 Terra 분담으로 구현·검증한다. 문서·README/웹 매뉴얼·전체 리팩토링·About·설치 배포는 단계별 후속 준비로 기록하며 모델 배포/자동 다운로드와 미검증 릴리즈는 하지 않는다.

2026-09-21 Discord 서버 추가 지시: 기존 테스트 서버에 더해 사용자가 지정한 새 서버 `1237999012301373450`도 허용한다. 두 서버 모두 채널 제한 없이 멤버 사용을 허용하며, 다른 서버와 DM 차단·소유자만 설치·공개 스포일러 결과·요청자 전용 상태 조회는 유지한다. 실제 적용 상태는 `docs/development/discord-personal-bot.md`의 서버 추가 기록을 따른다.

## 현재 단계

- 2026-09-22 사용자 승인: 전역 조각 관리 → 캐릭터·의상 → 생성 준비 → 진행 → 검증 결과 → 이미지셋의 UI를 구현하고 테스트·PR·원격 develop squash 병합까지 자율 진행한다. Terra에 구현·테스트를 분담한다. 조각은 사용자 관리 단일 계층 카테고리, 전역 고유 번호 자동 부여·변경/재사용 없음으로 구현하며 파일명 구조는 보류한다. 이미지셋은 캐릭터·의상별 검증 통과 결과 조회로 구성한다. 모던한 공통 스타일과 모바일 탐색·입력·상태 유지가 이번 범위이며 실제 모바일 기기 미검증은 구분한다. 이후 실사용 불편은 건별 보완한다.

- 2026-09-21 사용자 승인: Google OAuth는 후속으로 미룬다. 개인용 Frontend는 Cloudflare Access 로그인 후 Core 토큰을 반복 입력하지 않도록 서버의 비공개 설정을 사용하며, 설정 화면에서 연결 상태와 저장 토큰 교체를 제공한다. Core가 서명된 Access 사용자 인증을 검증하고 기존 서비스 Bearer 인증을 유지한다. 토큰 원문은 Frontend 설정 응답·정적 파일·브라우저 저장소·URL에 노출하지 않는다.

- 2026-09-21 최신 우선순위 변경: Discord 봇은 일반 테스트용으로 두고 후순위로 미룬다. 공개·스포일러·무작위 Seed와 사용값 표시의 사용자 확인은 완료했다. 본체 Backend와 F/E 제작 흐름을 기준으로 남은 구현 우선순위를 재검토한다. 과거 Discord 우선 지시를 현재 신규 기능 착수 순서로 계속 적용하지 않는다.

- 2026-09-21 Discord 권한 후속 지시: 봇 설치는 앱 소유자만 할 수 있게 제한하고, 지정한 서버/채널에서는 모든 멤버가 생성 명령을 사용할 수 있게 한다. 기존 본인/친구 User ID 열거 방식만으로 사용자를 제한하는 정책을 변경한다. 후속 확인으로 현재 테스트 서버 전체 멤버에게 허용하며 다른 서버와 DM은 허용하지 않는다. 추가 지시로 새 `/draw` 결과는 해당 채널에 공개하고 이미지 스포일러를 기본 적용한다. `/status`는 요청자 전용이며 타인의 상태/결과 조회는 허용하지 않는다.

- 2026-09-21 사용자 지시: 배포 전에는 사용자 한 명이 개인 제작에 사용한다. 외부 API 테스트는 보류하고 로컬/원격 동기화 후 Discord 봇 개발을 우선한다. Discord 요청은 F/E의 작품·캐릭터·의상 그룹과 독립적이며, 자연어를 로컬 LLM으로 프롬프트화하거나 자연어/Positive Prompt를 그대로 전달해 이미지를 응답한다. Cloudflare Workers를 사용하고 Discord 대기·응답 만료를 처리한다. Core의 상태·실행 조정 책임은 유지한다. 다중 사용자 배포·무인 대량 운영 준비를 개인용 봇의 선행 완료 조건으로 강제하지 않는다.

- 2026-09-13 최신 사용자 지시: 확정 설계에 따른 구현과 테스트는 단계별 승인 없이 계속 진행한다. 아래 과거 ADR 전용/구현 보류 기록은 당시 범위이며 최신 구현 지시를 제한하지 않는다. 설계가 확정된 구현·테스트는 Terra 서브에이전트에 위임하고 주 에이전트는 통합·검토 및 GPU 실행을 조정한다. 제품 의도나 확정 정책을 바꾸는 결정만 별도로 확인한다.

- 2026-09-13 후속 사용자 지시로 Frontend를 제외한 Backend 3개·Shared API Client·CLI 구현에 착수한다. 아래 과거 문서 전용/구현 보류 범위보다 이 지시가 우선한다. 일반 상세는 기존 요구에 맞춰 구현·문서화하며 추가 ADR 승인 질문으로 분할하지 않는다.
- Generation은 REST API로 Node를 제어한다. Node 구현·등록·필수 자원 준비 상태를 먼저 확인하고 준비된 Node부터 하나씩 REST 실행을 검증한다. 부분 prototype을 전체 기능 완료로 보고하지 않는다. GPU 실행은 주 에이전트가 순차 조정한다.

- Backend 아키텍처 및 기능 요구사항을 정리하면서 ComfyUI Custom Node 개발을 병행한다. 2026-09-12 사용자 지시로 설치된 Anima 기반 이미지 생성 Node를 먼저 구현·테스트한다. 주 에이전트가 구조를 정리하고 Terra 서브 에이전트가 개발·테스트 작성을 담당한다. [개발 범위](docs/modules/custom-nodes-design.md)를 따른다.
- 위 Custom Node 범위 외 제품 코드·실행 환경·의존성·CI는 명시적인 후속 지시 없이 추가하지 않는다. 미정 ADR·제품 정책을 첫 구현의 임시 선택만으로 확정하지 않는다.
- 미확정 내용을 임의로 결정하지 않는다. 대안과 권고는 확정된 결정과 구분하여 제시한다.
- ADR은 사용자와 하나씩 논의한다. 사용자가 확정하기 전에는 Accepted로 기록하지 않는다.
- 대화만으로 결정을 보관하지 않는다. 확정된 결정은 즉시 저장소 문서에 반영한다.
- 현재 1인 개발이다. 리뷰 기준과 브랜치 보호는 최초 릴리즈 이후 검토하도록 유예했으므로 지금 강제하거나 설정하지 않는다. 상세 상태는 [ADR-0001](docs/architecture/adr/0001-contribution-workflow.md)을 확인한다.

## Custom Node 개발 완료 기준

- 2026-09-13 사용자 요청: Custom Node 개발·테스트 후에는 설치된 ComfyUI에서 사용자가 직접 실행할 수 있도록 연결하고 예제 Workflow를 제공한다. 저장소 내부 테스트만 통과한 상태를 사용자 실행 준비 완료로 보고하지 않는다.
- 현재 대상 설치는 `C:\StabilityMatrix\Packages\ComfyUI`다. 경로·실행 상태를 확인하고 기존 설치·모델·사용자 Workflow를 보존한다. 같은 소스 연결은 재사용하며 다른 파일이나 Node를 덮어쓰지 않는다.
- 설치된 ComfyUI의 실제 Node 등록과 예제 Workflow 실행을 확인한다. 가능하면 화면에서 결과를 확인할 수 있게 준비하고, Node 이름·Workflow 위치·접속 주소 또는 실행 방법을 최종 응답에 제공한다.
- 실행 중인 사용자 작업을 임의로 중단하지 않는다. 재시작이 필요하면 작업 상태를 확인하고, 중단이 필요한 작업이 있으면 사용자와 조율한다. 설치·시작·재시작 및 실제 검증 여부를 구분해 기록한다.

## Validation 상세 설계 위임 — 2026-09-13

- 사용자는 나머지 Validation 설계를 기존 기능 요구사항·확정 ADR과 일반적인 기준으로 진행하고, 사용자 판단이 꼭 필요한 경우에만 질문하도록 위임했다. 현재는 **ADR 검토와 문서 작성 단계**다. 일상적인 필드·초기 운영값·복구 설계는 개별 승인 질문 없이 문서 초안으로 구체화한다. 테스트를 통한 조정은 후속 구현 단계이며 현재 Validation 코드·자동 검사·기계 Schema 생성으로 확장하지 않는다.
- 사용자 승인된 원칙과 에이전트가 정한 구현 초안/테스트값의 출처를 구분한다. 충돌 없는 세부 설계를 이유로 매번 중단하지 않는다. 제품 의도·기존 확정 책임·외부 전송/비용·삭제·자동 실행 경계 변경 또는 해소 불가 요구 충돌은 사용자에게 확인한다.
- 이 위임은 다른 모듈의 ADR 일괄 승인이나 Backend 구현·모델 다운로드·유료 API 실행 지시가 아니다. 현재 진입점은 docs/architecture/adr/0019-validation-contract-and-operational-draft.md다.

## 응답 방식

- 최종 응답에는 현재 상태에 맞는 다음 진행 작업 제안을 함께 제공한다.
- 항목 검토 시 설계상 문제가 있는 부분만 지적하고, 문제가 없으면 합의 상태와 필요한 결정만 간결히 답한다. 이미 확정된 내용을 반복 설명하지 않는다. 사용자가 상세 설명을 요청하면 필요한 범위로 설명한다.
- 다음 작업 제안은 미확정 결정의 승인이나 실행 지시로 간주하지 않는다. ADR은 계속 하나씩 논의한다.

## 필수 탐색

Git 작업과 메시지 작성에는 [CONTRIBUTING.md](CONTRIBUTING.md)의 확정 기준을 따른다. 미정 제안을 운영 규칙으로 취급하지 않는다.

작업 전에 [문서 목차](docs/README.md), [아키텍처 개요](docs/architecture/overview.md), [ADR 목록 및 절차](docs/architecture/adr/README.md), [미결정 목록](docs/architecture/adr/backlog.md)을 읽는다. 작업에 해당하는 [요구사항](docs/requirements/scope.md)과 [모듈 책임](docs/modules/README.md)도 확인한다.

## 아키텍처 원칙

- 단일 Monorepo 안에서 Core / Generation / Validation을 관리하며 각 서비스는 독립 실행 가능해야 한다.
- REST API가 canonical contract다. 별도 BFF는 두지 않으며 Shared API Client / SDK를 사용한다.
- Frontend와 CLI는 얇은 클라이언트로 유지한다. Backend 비즈니스 로직을 복제하지 않는다.
- Core가 SQLite 및 애플리케이션 주요 상태를 소유한다. 이미지와 대형 모델 파일은 Filesystem에 저장한다.
- Generation의 생성 연동 대상은 ComfyUI다. Generation은 SQLite 및 AI Provider에 직접 접근하지 않는다.
- AI 및 Agent의 Core 작업은 허용된 Core REST API를 통한다. SQLite 또는 임의 SQL 실행 권한을 직접 제공하지 않는다.
- 외부 AI에는 요청 수행에 필요한 최소 Context만 전달한다.

상세 범위, 예외 및 미결정 사항은 아키텍처·요구사항·ADR 문서를 확인한다. 이 파일에 모든 설계를 복제하지 않는다.

## 결정 반영 절차

ADR 확정 시 해당 파일 작성 또는 갱신 → overview 갱신 → 기존 ADR 충돌 확인 → 변경된 기존 ADR 상태와 supersede 관계 기록 → 관련 요구사항·모듈 문서 및 backlog 갱신을 수행한다.

문서끼리 충돌하면 임의로 결론을 선택하지 말고 충돌 지점과 확인이 필요한 결정을 제시한다. 기존 사용자 변경은 보존한다. Branch, Commit, Test, CI 등의 미정 정책을 이미 확정된 규칙처럼 도입하지 않는다.
